import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.droid.musicplayer',
  appName: 'youtube-music-player',
  webDir: 'dist'
};

export default config;
